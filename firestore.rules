rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read/write access to all documents for now (Development Mode)
    // WARNING: This is insecure for production. Replace with proper auth rules later.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}

